# Project Rules

- **Full Workflow Execution on Modifications**: Whenever the user requests any code/text modifications, perform the complete process:
  1. Make the necessary code edits.
  2. Verify the changes (if applicable).
  3. Git stage, commit the changes with a clear commit message.
  4. Push the changes to the GitHub remote repository (`origin`).
